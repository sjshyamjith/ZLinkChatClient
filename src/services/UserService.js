const login = (email, password) => {
  //connect api
  return true;
};

const register = (fullName, email, password) => {
  //connect api
  return { id: 1, fullName, email, password };
};

export default { login, register };
